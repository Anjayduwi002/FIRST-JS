et n = 5; // ubah nilai ini sesuai dengan ukuran simbol yang ingin dibuat
let symbol = '';
for (let i = 0; i < n; i++) {
  for (let j = 0; j < n; j++) {
    if (i === n / 2 || j === n / 2) {
      symbol += '*';
    } else {
      symbol += ' ';
    }
  }
  symbol += '\n';
}
console.log(symbol);